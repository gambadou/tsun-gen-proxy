name: "TSUN Gen3 Proxy"
version: "1.0.0"
slug: "tsun_gen3_proxy"
description: "Proxy pour récupérer les données des onduleurs TSUN Gen3"
arch:
  - amd64
  - armv7
  - armhf
  - aarch64
startup: application
boot: auto
options:
  mqtt_host: "homeassistant"
  mqtt_port: 1883
  mqtt_user: "mqtt"
  mqtt_password: "mqtt"
schema:
  mqtt_host: str
  mqtt_port: int
  mqtt_user: str
  mqtt_password: str